services:
  backend:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - hf_cache:/root/.cache/huggingface
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload

volumes:
  hf_cache:
